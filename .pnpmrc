# pnpm配置优化 - 解决VuePress + Vue依赖解析问题

# 强制Vue相关包提升到根级别
public-hoist-pattern[]=vue
public-hoist-pattern[]=@vue/*
public-hoist-pattern[]=vue-router

# 减少peer dependency的严格性
strict-peer-dependencies=false

# 使用提升链接模式以获得更好的兼容性
node-linker=hoisted

# 自动安装peer dependencies
auto-install-peers=true

# 禁用幻影依赖警告（针对VuePress生态）
strict-peer-dependencies=false